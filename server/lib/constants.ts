// ── Shared Server Constants ────────────────────────────────────────────────────
// All magic numbers, thresholds, and duplicated constants live here.
// Server routes import from this file instead of hardcoding values.

export const DEFAULT_ADMIN_CUT_PERCENT = 10;
export const MIN_ORDER_AMOUNT_USD = 3;
